# 大学平行时空 University Parallel Universe

## 1. 项目定位

**大学平行时空** 是一个面向高中生的 Web 交互体验项目。

项目通过“选择专业 → 模拟一天 → 做出生活选择 → 生成校园路线和个人画像”的方式，让高中生直观感受不同专业的大学生活节奏。

它的交互形式可以类比 MBTI 测试问卷：用户在一天中的多个时间段做选择，每个选择都会影响若干维度的分数，最终生成一个属于用户的“专业一日体验画像”。

## 2. 核心操作逻辑

项目流程简化为：

```text
进入 Web App
→ 选择一个专业
→ 进入该专业的一日日程模拟
→ 在有限个时间段中做选择
→ 每个选择绑定一个事件和校园地点
→ 根据选择更新多维度分数
→ 在可视化界面形成一天的校园路线
→ 最终总结专业、路线和个人画像
```

本项目不做复杂推荐，也不需要一开始根据兴趣匹配专业。用户先直接选择想体验的专业，这样实现更简单，展示也更稳定。

本项目视觉方向固定为：

- 等距校园插画风
- 路线发光连线
- 轻手账式信息卡片

## 3. MVP 功能范围

一周内优先完成以下功能：

- 专业选择
- 一日日程模拟
- 多时间段事件选择
- 多维度评分
- 事件与校园地点绑定
- 简化校园路线可视化
- 最终画像总结

暂时不做：

- 用户登录
- 数据库
- 自动爬虫
- 机器学习推荐
- 真实地图导航
- 大量专业覆盖
- 复杂专业对比

可选加分功能：

- 课程问答助手：用户围绕某个专业的核心课程提问，系统基于本地课程数据和 AI API 按固定格式给出简短回答。

## 4. 专业范围

MVP 阶段建议先做 3 个专业，保证差异明显即可。

推荐首批专业：

- 计算机类
- 医学类
- 经管类

如果时间充足，再加入：

- 法学类
- 建筑 / 设计类

每个专业需要准备一套对应的一日日程模板。

## 5. 一日日程模拟设计

一天被拆分为有限个时间段，每个时间段对应一个问题或事件选择。

建议控制在 5-7 个时间段，避免问卷太长。

示例时间段类型：

```text
早晨
上午
中午
下午
傍晚
晚上
睡前
```

每个时间段提供 2-4 个选项。

每个选项包含：

- 选项文案
- 对应事件
- 对应校园地点
- 对各评分维度的影响

## 6. 评分维度

建议使用 4-5 个维度，保持清晰。

推荐维度：

```text
健康平衡 health
学习投入 study
社交丰富 social
实践探索 practice
压力指数 pressure
```

每个选择会让某些维度增加或减少。

例如：

```json
{
  "label": "去图书馆继续自习",
  "event": "自习",
  "locationId": "library",
  "score": {
    "health": 0,
    "study": 2,
    "social": 0,
    "practice": 0,
    "pressure": 1
  }
}
```

最终根据总分生成用户画像。

画像示例类型：

```text
高专注学习型
均衡探索型
社交活跃型
实践驱动型
压力拉满型
```

## 7. 路线可视化逻辑

每个事件都绑定一个校园地点。

用户每做一次选择，就把该事件对应地点加入当天路线。

最终形成：

```text
宿舍 → 教学楼 → 食堂 → 图书馆 → 操场 → 宿舍
```

为了控制开发难度，MVP 阶段使用 **简化校园地图**：

- 不接真实地图 API
- 用一个自定义的等距校园插画底图表示校园
- 用固定点位表示地点
- 用发光线连接用户当天经过的地点

地图表现要求：

- 保持轻微立体感或伪 3D 感
- 建筑和区域关系清晰
- 底图不抢文字信息，但要有校园氛围
- 点位、编号和路线比底图更醒目

地点数据可以写在 `locations.json` 中。

示例：

```json
{
  "id": "library",
  "name": "图书馆",
  "x": 58,
  "y": 35,
  "type": "study"
}
```

其中 `x` 和 `y` 表示地点在简化地图中的相对位置百分比。

## 8. 数据结构

### 8.1 专业数据 majors.json

```json
[
  {
    "id": "computer",
    "name": "计算机类",
    "description": "偏重逻辑思维、项目实践和持续学习。",
    "tags": ["逻辑", "编程", "项目", "实践"]
  }
]
```

### 8.2 地点数据 locations.json

```json
[
  {
    "id": "dorm",
    "name": "宿舍区",
    "x": 15,
    "y": 70
  },
  {
    "id": "teaching_building",
    "name": "教学楼",
    "x": 42,
    "y": 42
  },
  {
    "id": "library",
    "name": "图书馆",
    "x": 62,
    "y": 35
  }
]
```

### 8.3 日程模板 experienceTemplates.json

每个专业对应一套时间段问题。

```json
{
  "computer": [
    {
      "id": "morning",
      "period": "早晨",
      "question": "新的一天开始了，你准备怎么进入状态？",
      "options": [
        {
          "label": "提前到教学楼预习课程",
          "event": "课前预习",
          "locationId": "teaching_building",
          "score": {
            "health": 0,
            "study": 2,
            "social": 0,
            "practice": 0,
            "pressure": 1
          }
        }
      ]
    }
  ]
}
```

实际开发时，每个专业准备 5-7 个时间段即可。

## 9. 页面结构

项目控制在 4 个页面。

```text
首页 / 专业选择页
一日模拟页
路线可视化页
结果画像页
```

也可以把路线和结果放在同一个页面，进一步简化。

### 9.1 首页 / 专业选择页

功能：

- 展示项目名称
- 简要说明玩法
- 展示专业卡片
- 用户选择专业后进入模拟

### 9.2 一日模拟页

功能：

- 显示当前时间段
- 显示当前问题
- 显示选项卡片
- 用户点击选项后进入下一时间段
- 页面上可以显示当前已积累的简要分数

### 9.3 路线可视化页

功能：

- 根据用户选择过的地点生成路线
- 展示地点点位
- 展示地点之间的连线
- 展示用户一天经过的地点顺序

### 9.4 结果画像页

功能：

- 展示用户选择的专业
- 展示五个维度最终分数
- 生成用户画像名称
- 展示用户一天的选择摘要
- 提供重新体验按钮

## 10. 推荐技术栈

为了控制在一周内完成，建议使用纯前端方案。

```text
React
Vite
JavaScript
CSS
本地 JSON 数据
```

不建议一开始做独立后端。

如果选择接入 API key 实现课程问答，采用最简单的轻量后端代理方案：

```text
前端 React
→ 调用本项目后端 /api/course-chat
→ 后端读取本地课程数据
→ 后端携带 API key 调用 AI 服务
→ 返回固定格式回答
```

API key 只放在后端 `.env` 文件中，不能写在前端代码里。

## 11. 项目结构

```text
University_Parallel_Universe/
├─ web/
│  ├─ package.json
│  ├─ index.html
│  ├─ vite.config.js
│  │
│  ├─ src/
│  │  ├─ main.jsx
│  │  ├─ App.jsx
│  │  ├─ styles.css
│  │  │
│  │  ├─ pages/
│  │  │  ├─ Home.jsx
│  │  │  ├─ Experience.jsx
│  │  │  └─ Result.jsx
│  │  │
│  │  ├─ components/
│  │  │  ├─ MajorCard.jsx
│  │  │  ├─ OptionCard.jsx
│  │  │  ├─ CampusMap.jsx
│  │  │  └─ ScorePanel.jsx
│  │  │
│  │  ├─ data/
│  │  │  ├─ majors.json
│  │  │  ├─ locations.json
│  │  │  └─ experienceTemplates.json
│  │  │
│  │  └─ utils/
│  │     ├─ score.js
│  │     ├─ profile.js
│  │     └─ route.js
│  │
│  └─ README.md
│
├─ backend/                     # 可选：课程问答 AI 代理后端
│  ├─ app.py                    # FastAPI/Flask 入口
│  ├─ requirements.txt
│  ├─ .env.example              # 环境变量示例，不填写真实 key
│  │
│  ├─ data/
│  │  └─ coreCourses.json       # 从“核心课程”sheet 导出的课程知识数据
│  │
│  ├─ routes/
│  │  └─ course_chat.py         # /api/course-chat
│  │
│  └─ services/
│     ├─ course_retriever.py    # 按专业和关键词筛选课程
│     └─ ai_client.py           # 调用 AI API
│
├─ docs/
│  ├─ demo-script.md
│  └─ ppt-outline.md
│
└─ PROJECT_PLAN.md
```

## 12. 三人分工

当前分工整体是合理的，但建议再明确成“主责 + 协作边界”的形式，避免地图和 AI 模块在后期没人接住。

### 成员 A：主前端与交互流程

负责：

- 搭建 React + Vite 项目
- 首页 / 专业选择页
- 一日模拟页
- 页面跳转和状态管理
- 基础 UI 样式
- 结果页整体排版
- 统一卡片视觉为轻手账式信息卡片

协作边界：

- 接收成员 B 提供的数据结构和评分结果
- 接收成员 C 提供的地图组件并接入结果页
- 后续如果接课程问答，负责把问答框和回答卡片挂到结果页

重点：

```text
保证用户可以顺利完成从选择专业到完成模拟的流程。
```

### 成员 B：数据结构、评分逻辑与 AI 后端预留

负责：

- 设计专业数据
- 设计日程模板数据
- 编写评分函数
- 编写画像生成函数
- 管理用户选择结果
- 整理 `核心课程` 数据，导出 `coreCourses.json`

可选加分职责：

- 搭建轻量 `backend/`
- 配置 `.env` 中的 AI API key
- 实现 `POST /api/course-chat`
- 做本地关键词匹配兜底回答

协作边界：

- 先保证前端主流程只依赖本地 JSON 就能跑通
- AI 后端必须在主流程稳定后再接入，不反向阻塞前端演示

重点：

```text
保证每个选项都能正确影响维度分数，并能生成最终画像。
如果接入 AI，保证后端只做“课程问答代理”，不扩大成复杂服务。
```

### 成员 C：地图模块、数据清洗与展示材料

负责：

- 设计简化校园地点
- 编写 `locations.json`
- 完成 `CampusMap` 组件
- 设计路线点位、编号、连线和高亮方式
- 清洗活动样本数据，统一地点和活动类型
- 准备 README、PPT 和讲稿

地图模块亮点方向：

- 使用人工标注的等距校园插画底图作为背景
- 用相对坐标定位地点
- 根据用户选择动态生成发光路线
- 用顺序编号和高亮节点强化“平行时空中的一天”体验

协作边界：

- 地图模块优先做“可展示、可连线、可读”的版本
- 不做真实导航，不接复杂地图 API

重点：

```text
保证用户选择的事件能在地图上形成清晰路线。
把地图做成前端展示亮点，但不让它拖慢主流程开发。
```

### 协作建议

- A 和 B 先约定 `selectedOptions`、`scores`、`route` 的状态结构
- B 和 C 先约定 `locationId`、`activity_type`、`course_name` 的命名
- C 提供地图点位后，A 只负责挂载组件，不重复改地图逻辑
- B 的 AI 后端接口固定成一个 `/api/course-chat`，避免前端联调面过大

## 13. 一周开发计划

### 第 1 天：确认结构与搭建项目

- 确定首批专业
- 确定评分维度
- 确定时间段数量
- 搭建 React + Vite 项目
- 创建基础页面和数据文件

### 第 2 天：完成专业选择和基础数据

- 完成首页 / 专业选择页
- 写好 majors.json
- 写好 locations.json
- 确定每个专业的模板结构

### 第 3 天：完成一日模拟

- 完成 Experience 页面
- 展示时间段问题
- 展示选项
- 记录用户选择
- 实现下一题逻辑

### 第 4 天：完成评分和画像

- 实现分数累计
- 实现画像生成
- 完成结果页基础展示
- 显示用户选择摘要

### 第 5 天：完成路线可视化

- 完成 CampusMap 组件
- 根据地点顺序画点和线
- 在结果页展示路线
- 调整视觉效果

### 第 6 天：美化和测试

- 优化页面样式
- 修复逻辑问题
- 检查完整流程
- 准备稳定 Demo 数据

### 第 7 天：展示准备

- 编写 README
- 准备 PPT
- 准备讲稿
- 录制备用演示视频
- 进行展示彩排

### 可选加分：课程问答

在核心 MVP 稳定后再做。

- 将 `核心课程` sheet 导出为 `coreCourses.json`
- 搭建轻量 `backend/`
- 在 `.env` 中配置 AI API key
- 实现 `POST /api/course-chat`
- 前端添加一个简单课程问答输入框
- 如果 AI 调用失败，使用本地关键词匹配返回兜底回答

## 14. 主要难点

### 14.1 状态管理

难点：

- 需要记录当前专业、当前时间段、用户选择、分数和路线。

解决方式：

- 所有关键状态集中放在 `App.jsx`
- 暂时不用复杂状态管理库
- 用 React `useState` 即可

### 14.2 数据结构设计

难点：

- 如果选项、分数、地点字段不统一，后续页面会很难对接。

解决方式：

- 先固定 JSON 字段
- 所有专业模板使用同一结构
- 不同成员写数据时必须遵守同一格式

### 14.3 路线可视化

难点：

- 真实地图开发成本较高。

解决方式：

- 使用简化地图
- 坐标用百分比定位
- 用 SVG 或 CSS 连线

### 14.4 AI Coding 整合

难点：

- AI 生成的代码可能风格不一致。

解决方式：

- 每次只让 AI 生成一个组件或函数
- 先定数据结构，再生成代码
- 每写完一个模块立刻运行测试

### 14.5 课程问答 API key 接入

难点：

- API key 不能暴露在前端
- AI 回答可能发散
- 网络或额度问题可能导致接口失败

解决方式：

- API key 只写在后端 `.env`
- 前端只请求 `/api/course-chat`
- 后端 prompt 固定回答格式
- 回答只基于 `coreCourses.json`
- 必须保留本地关键词匹配兜底

## 15. 项目一句话介绍

**大学平行时空是一个面向高中生的 Web 专业一日模拟项目，用户选择专业后，通过类似 MBTI 问卷的分支选择体验一天的大学生活，并最终生成校园路线和多维度个人画像。**
